# Lab 05 — File Contents and Comparing

**Area:** Linux  
**Completed:** 2026-08-26

## What I learned
- Used `cat` and `cat -n` to inspect files.
- Used `head` and `tail` by lines and bytes.
- Compared files with `diff` and directories with `diff -r`.
- Used `man` pages as command references.
