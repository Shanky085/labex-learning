# Lab 22 — Save a Terminal Banner

**Area:** Linux

## What I learned
- Worked in `/home/labex/project/terminal-greeting`.
- Used the default `figlet` font to render `Welcome`.
- Saved the exact ASCII-art output to `welcome-banner.txt` using redirection.
