# Lab 11 — The Lay of the Land

**Area:** Linux  
**Completed:** 2026-08-29

## What I learned
- Used `whoami`, `uname`, `who`, and `id` for system/user inspection.
- Used `top` to monitor processes and resource usage.
- Used `>` and `>>` to redirect output into reports.
