# Lab 08 — Change File Ownership

**Area:** Linux

## What I learned
- Created files with `touch`.
- Changed ownership with `chown`.
- Modified permissions with `chmod`.
- Viewed file details with `ls`.
