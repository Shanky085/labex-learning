# Lab 33 — Get Help on Linux Commands

**Area:** Linux

## What I learned
- Used `type` or `command -V` to identify a command or name.
- Used Bash `help` and program `--help` for quick syntax.
- Used `man` for complete documentation.
- Used `apropos` when the topic was known but the command was not.
