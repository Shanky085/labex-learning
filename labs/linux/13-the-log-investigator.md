# Lab 13 — The Log Investigator

**Area:** Linux

## What I learned
- Used `grep` to filter log files and find errors.
- Used `dmesg` for system-level and kernel diagnostics.
- Used `diff` to compare configuration files.
- Used pipelines and redirection to process and document findings.
