# Lab 20 — Git Branch Basic Operations

**Area:** Git

## What I learned
- Created and switched between branches with `git checkout` or `git switch`.
- Committed changes on a separate branch.
- Merged branches into the main timeline.
- Deleted branches that were no longer needed.
