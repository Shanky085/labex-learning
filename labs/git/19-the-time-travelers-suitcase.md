# Lab 19 — The Time Traveler's Suitcase

**Area:** Git

## What I learned
- Initialized a Git repository.
- Created a file and staged the change.
- Used `git diff --staged` to inspect exactly what was prepared for commit.
