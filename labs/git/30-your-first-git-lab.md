# Lab 30 — Your First Git Lab

**Area:** Git

## What I learned
- Created a Git repository with `git init`.
- Checked repository state with `git status`.
- Staged files with `git add`.
- Created commits with `git commit`.
- Reviewed project history with `git log`.
