# Lab 18 — Working with Files and Staging Area

**Area:** Git

## What I learned
- Used `git add` to stage files.
- Used `.gitignore` to exclude files from tracking.
- Used `git diff` to review changes.
- Used `git restore --staged` to unstage changes.
- Practiced selective, focused commits.
