# Lab 32 — Git Config Management

**Area:** Git

## What I learned
- Used `git config` to manage Git settings.
- Configured Git identity and color output.
- Set Nano as the default editor.
- Configured line-ending behavior and aliases.
- Practiced experiment-specific repository configuration.
