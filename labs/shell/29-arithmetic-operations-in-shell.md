# Lab 29 — Arithmetic Operations in Shell

**Area:** Shell

## What I learned
- Used the Bash shebang `#!/bin/bash`.
- Assigned variables without spaces around `=`.
- Performed arithmetic with `$(( ))`.
- Used `echo` for output.
- Made scripts executable with `chmod +x` and ran them from the command line.
