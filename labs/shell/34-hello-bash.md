# Lab 34 — Hello Bash

**Area:** Shell

## What I learned
- Used the WebIDE terminal and editor to create a Bash script.
- Learned the purpose of the shebang line.
- Used `echo` to print text.
- Used `chmod` to make the script executable and ran it from the command line.
- Used `cat` to view file contents.
