# Lab 26 — Passing Arguments to the Script

**Area:** Shell

## What I learned
- Created executable scripts that accept command-line arguments.
- Used positional parameters such as `$1` and `$2`.
- Used `$#` to count arguments and `$@` to access all arguments.
- Applied conditional logic and loops to process supplied arguments.
